{
  "name": "notify-unresolved-threads-action",
  "version": "1.0.0",
  "description": "GitHub Action to notify reviewers of unresolved threads after approval",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "build": "ncc build index.js --license licenses.txt",
    "prepare": "npm run build",
    "format": "prettier --write '**/*.{js,json,md,yml}'",
    "lint": "eslint index.js src/"
  },
  "keywords": [
    "github-action",
    "pull-request",
    "review",
    "threads"
  ],
  "author": "kreuz123",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^2.0.3",
    "@actions/github": "^8.0.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "@vercel/ncc": "^0.45.0",
    "eslint": "^9.39.5",
    "globals": "^17.11.0",
    "jest": "^30.4.2",
    "prettier": "^3.9.6"
  }
}
